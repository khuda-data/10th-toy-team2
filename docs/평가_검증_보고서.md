# 평가·검증·통합 보고서

> 2026-08-22 · 권동하
> 입력: `docs/모델링_가이드.md`(전처리), `docs/모델링_진행기록.md`(경사·개인화 모델)
> 코드: `src/eta.py`(통합 엔진), `src/evaluate_internal.py`(검증 실험)

---

## 1. 핵심 결론 — 발표 헤드라인

**실제 검증 경로 12회차에서, 사용자 이력이 있으면 기존 지도앱 방식 대비 도착시간 오차를 81% 줄인다 (147초 → 28초).**

| 시나리오 | MAE | MAPE | 정속 대비 | 유의성 |
|---|---|---|---|---|
| M0 정속 4km/h (기존 지도앱) | 147초 | 16.4% | — | — |
| M3 콜드스타트 — 처음 보는 사용자 | 84초 | 9.1% | 43% 감소 | p = 0.055 ❌ |
| **M4 웜스타트 — 이력 3회 보유** | **28초** | **2.9%** | **81% 감소** | **p = 0.0016 ✅** |

- 평가 대상: 독립 검증 경로 2개 × 4명 × 최대 2회 = **정방향 12회차** (전부 학습 미사용)
- 별도로 역방향 4회차(박준서·홍민기 2회차 = 돌아오는 길)가 있으며 11-3절에서 따로 다룬다
- 정답은 `dt_s`(총 경과시간). 신호대기는 스톱워치 실측 사용

### 이 프로젝트의 핵심 발견

**개인화의 성패는 v_user를 "어느 맥락에서" 학습했는지가 좌우한다.**

경사 실험용 반복 보행에서 뽑은 v_user는 목적지를 향한 연속 보행보다 최대 21% 느리다
(박준서 1.301 → 1.572 m/s). 이 도메인 시프트 때문에 콜드스타트는 전원 과대예측하고
(+53초 계통 편향), 박준서 한 명에서는 **정속 baseline보다도 나빠진다**(141초 vs 89초).

같은 맥락의 이력 3회로 v_user를 다시 잡으면(누수 없는 회차 leave-one-out) 오차가
84초 → 28초로 줄고 계통 편향이 −1초로 사라진다. 즉 **개인화는 "그 사람의 속도"를
아는 것이 아니라 "그 사람이 그런 상황에서 내는 속도"를 아는 것**이다.

이는 서비스 설계에 직접적인 함의가 있다 — 신규 사용자에게는 개인화를 켜지 말고
팀평균·정속으로 시작해, 같은 맥락 이력이 3회 정도 쌓인 뒤 개인화를 켜야 한다.

> 참고 — 내부 검증(학습 데이터 회차 단위 홀드아웃 20%, 10-seed)에서는
> MAE 38.1초 → 14.7초 (61% 감소, p = 5.2e-04)였다(4절).
> 학습·평가가 같은 맥락이므로 위의 웜스타트(81%)에 대응하는 수치이며,
> 콜드스타트(43%)와의 격차가 곧 도메인 시프트의 크기다.

## 2. 통합 결과물 — ETA 엔진 (`src/eta.py`)

세 파트의 산출물을 하나의 함수로 조립했다.

```
ETA = Σ [ dist_m / (v_user × k_slope(slope_pct)) ] + Σ 신호대기
        └── 속도 모델 ──┘                            └ 신호 모델 ┘
```

| 항목 | 처리 |
|---|---|
| k_slope | `models/k_slope_model_v2.json` 자동 인식, 없으면 v1 fallback |
| v_user | `models/v_user_v2.csv` |
| 외삽 방지 | 경사도를 `valid_slope_range`로 클립 후 배율 [0.3, 1.4] 제한 (v2 문서 규약 준수) |
| 신호대기 | `data/processed/영통구_횡단보도별_신호대기.csv`(351개 지점) 자동 인식, 없으면 0초로 동작 |
| 신규 사용자 | v_user에 없는 사람은 팀 평균 속도로 fallback |

동작 예시 (김재형\_급경사내리막\_1회차, 161m):
```
실측 115초 | WalkFit 116초 (오차 1초) | 정속 4km/h 145초 (오차 30초)
```

### 신호대기 연동 (완료)

신호대기 산출물 `data/processed/영통구_횡단보도별_신호대기.csv`(351개 지점, 신호 있는 곳 300개)를 엔진에 연결했다. 컬럼명이 사전 합의안(`crossing_id, lat, lon, cycle_C_s, red_R_s`)과 달라(`횡단보도관리번호, 위도, 경도, 주기_s, 적색_R_s`) 한글 스키마도 인식하도록 수정했으며, 이미 산출된 `기대대기_s`를 그대로 사용한다.

엔진은 경로 구간 경계가 횡단보도 반경 25m 내를 지나면 해당 지점 기대대기를 합산한다.

## 3. 검증 방법론 — 두 가지 누수 차단

기존 문서의 모든 R²는 in-sample(학습 데이터 자기 채점)이었다. 이를 out-of-sample 평가로 대체하면서 두 가지 데이터 누수를 차단했다.

**(1) 분할 단위를 구간이 아닌 회차(파일)로.** 같은 GPX 파일의 인접 구간은 같은 날·같은 길·같은 컨디션이라 강하게 상관된다. 구간을 랜덤 분할하면 train에 있는 구간의 바로 옆 구간이 test에 들어가 성능이 과대평가된다. 파일 단위로 분리해야 "처음 걷는 길"에 대한 성능이 된다.

**(2) 파이프라인 전체를 train만으로 재적합.** 배포된 `v_user_v2.csv`를 그대로 쓰면 그 값 자체가 test 구간까지 포함해 계산된 것이라 누수다. 매 분할마다 평지속도 → 균형표본 → k_slope → v_user를 train 세트만으로 다시 계산한 뒤 test를 평가한다.

## 4. 구간 단위 검증 — 개인화가 실제로 기여하는가

회차 단위 홀드아웃, 10-seed 평균 구간속도 MAE (m/s, 낮을수록 좋음):

| 방식 | MAE | 의미 |
|---|---|---|
| 정속 4km/h | 0.2660 ± 0.0150 | 기존 지도앱 |
| (a) 공통곡선만 | 0.1674 ± 0.0094 | 경사만 반영, 개인화 없음 |
| **(b) v2 개인화** | **0.1601 ± 0.0086** | 현재 채택 모델 |
| (c) shrinkage 적용 | 0.1611 ± 0.0090 | 축소추정 변형 |

**개인화는 실제로 기여한다.** (a) 0.1674 → (b) 0.1601. 진행기록 7절에서 제기된 "v_user가 v_flat_raw와 근접해 개인화 효과가 사라질 가능성"은 out-of-sample에서도 개선이 유지되므로 해당하지 않는 것으로 확인됐다.

## 4-1. Shrinkage(계층적 축소추정) — 표본 크기별 손익분기점

당초 기획의 계층적 축소추정을 (c)로 구현해 비교한 결과 (b)보다 나아지지 않았다. 다만 **"한 데이터셋에서 개선이 없다"는 이유만으로 방법론을 폐기하는 것은 부당**하므로, 표본 크기를 인위적으로 줄여가며 shrinkage가 유효해지는 조건을 탐색했다.

**실험**: 각 사람의 train 구간을 5·10·30개로 제한한 뒤, shrinkage 적용/미적용의 test MAE를 20-seed 대응표본 t-검정으로 비교.

| 사람당 train 표본 | shrinkage 없음 | shrinkage 있음 | 차이 | p-value | 판정 |
|---|---|---|---|---|---|
| **현재(72~363개)** | **0.1674** | 0.1714 | −0.0039 | <0.001 | 미적용이 유의하게 우세 |
| 30개 | 0.1688 | 0.1729 | −0.0041 | <0.001 | 미적용이 유의하게 우세 |
| 10개 | 0.1704 | 0.1737 | −0.0033 | 0.057 | 구분 불가 |
| 5개 | 0.1895 | **0.1858** | **+0.0038** | 0.061 | 부호 역전 — 적용이 우세한 경향 |

(차이 = 미적용 − 적용. 양수면 shrinkage가 유리)

**결론.** 표본이 줄어들수록 shrinkage의 상대적 성능이 단조 개선되며, **5개 구간 부근에서 부호가 역전**된다(−0.0039 → −0.0041 → −0.0033 → **+0.0038**). 즉 shrinkage는 표본이 극히 적을 때 유효한 방법이고, 손익분기점은 **사람당 약 5~10개 구간** 사이다. 본 프로젝트는 최소 72개(train 기준)로 이 지점을 크게 상회하므로, 팀 평균으로 끌어당기는 보정이 오히려 개인 실측값을 왜곡한다.

(다만 5·10개 조건의 p-value가 0.06 수준으로 통상적 유의수준에 미달하므로, "부호가 역전되는 경향을 확인했다"까지가 정확한 진술이며 "5개에서 shrinkage가 유의하게 낫다"고 단정하지 않는다.)

> **채택 결정**: 현재 데이터에는 shrinkage를 **미적용**한다(v2 방식 유지).
> **확장 권고**: 서비스화 시 신규 사용자처럼 측정 이력이 10개 미만인 경우에는 shrinkage를 적용하는 이원 설계가 타당하다. 즉 shrinkage는 "틀린 방법"이 아니라 "적용 조건이 있는 방법"이며, 본 실험으로 그 조건을 정량화했다.

재현: `python -m src.evaluate_internal --shrinkage-curve --n-seeds 20`

## 4-2. 개인 데이터량에 따른 학습곡선 — 더 걸으면 무엇이 좋아지는가

윤예진·홍민기 두 참여자는 다른 인원(89~97개)보다 많은 데이터를 수집했다(146개·454개). "데이터가 쌓이면 개인화가 좋아진다"는 기획 가설을 이 불균형 구도로 검증했다.

**실험 설계**: 대상자의 파일 20%를 test로 고정하고, 남은 train에서 n개를 무작위 추출해 `v_user`를 추정한 뒤 test 구간속도 예측오차를 측정한다. 공통 `k_slope` 곡선은 **대상자를 제외한 나머지 인원 데이터로 고정**해, 개인 표본 수의 효과만 분리했다. 같은 n을 40-seed 반복했을 때 `v_user` 추정값이 흔들리는 정도(표준편차)를 1.2km 경로 ETA의 ±초로 환산해 신뢰구간으로 삼았다.

### 결과 — 정확도는 포화, 신뢰도는 계속 개선

| 홍민기 (보유 454개) | 예측오차 MAE | ETA 신뢰구간 |
|---|---|---|
| 0개 (팀평균) | 0.2224 | — |
| 5개 | 0.2235 | ± 69초 |
| 30개 | 0.2123 | ± 34초 |
| 90개 *(타 인원 수준)* | 0.2113 | ± 17초 |
| 250개 | 0.2113 | ± 10초 |
| **454개 (현재)** | 0.2121 | **± 8초** |

| 윤예진 (보유 146개) | 예측오차 MAE | ETA 신뢰구간 |
|---|---|---|
| 0개 (팀평균) | 0.1375 | — |
| 5개 | 0.1585 | ± 56초 |
| 30개 | 0.1381 | ± 20초 |
| 90개 | 0.1357 | ± 11초 |

**확인된 사실 두 가지**

1. **예측오차는 약 20~30개 구간에서 포화된다.** `v_user`는 평균 하나를 추정하는 값이므로 표준오차가 1/√n으로 수렴하며, 30개면 평균값 자체는 충분히 잡힌다. 추가 수집이 예측값을 더 정확하게 만들지는 않는다.
2. **반면 추정의 신뢰도는 계속 개선된다.** 홍민기의 ETA 신뢰구간은 5개일 때 ±69초에서 454개에서 **±8초로 88% 축소**됐다(윤예진 ±56초 → ±11초). 데이터를 더 모아서 얻는 것은 "더 정확한 값"이 아니라 **"그 값을 더 확신할 수 있다"**는 점이다.

이는 서비스 관점에서 의미가 있다. 이용 이력이 적은 사용자에게는 "약 15분"으로, 이력이 충분한 사용자에게는 "15분 ±8초"로 제시하는 등 **불확실성을 함께 표현하는 근거**가 된다.

### 개인화의 가치는 데이터 양보다 '평균에서의 이탈'이 결정한다

팀 평균만 쓴 경우(n=0) 대비 개인화 적용 시의 개선율:

| 참여자 | v_user | 팀평균과 차이 | 팀평균 MAE | 개인화 MAE | 개선 |
|---|---|---|---|---|---|
| **최희수** | 1.627 | **+18.3%** | 0.3001 | 0.1316 | **+56%** |
| 김재형 | 1.294 | −5.9% | 0.1832 | 0.1645 | +10% |
| 홍민기 | 1.332 | −3.1% | 0.2224 | 0.2113 | +5% |
| 박준서 | 1.301 | −5.4% | 0.1184 | 0.1072 | +9% |
| 윤예진 | 1.335 | −2.9% | 0.1375 | 0.1357 | +1% |
| 권동하 | 1.360 | −1.1% | 0.1148 | 0.1136 | +1% |

최희수는 **5개 구간만으로 오차를 56% 줄였다**(0.3001 → 0.1364). 반면 팀 평균에 가까운 인원은 데이터를 아무리 모아도 개선이 1~5%에 그친다. 즉 개인화가 필요한 대상은 "많이 걸은 사람"이 아니라 **"평균과 다른 사람"**이다.

재현: `python -m src.evaluate_internal --learning-curve --n-seeds 40`
(원자료: `data/processed/eval_learning_curve.csv`)

## 5. v1 vs v2 비교 — 어느 버전을 쓸 것인가

v2(1차식 + 경사보정 v_user)를 그대로 채택하지 않고 독립 평가했다.

| 모델 | 경로 MAE | 경로 MAPE |
|---|---|---|
| v1 (2차식 + shrinkage) | 15.3초 | 7.1% |
| **v2 (1차식 + 경사보정)** | **15.2초** | **7.1%** |

성능이 동등하면서 v2가 더 단순하고(1차식) 검증 방법이 더 정직하므로(CV 기반) **v2 채택이 타당함을 확인**했다. 통합 엔진은 v2를 기본값으로 사용한다.

## 6. 검증 데이터 수집 설계 (확정)

**독립된 편도 경로 2개**를 팀원 6명이 각각 2회씩 걷는다. 총 **24개 GPX**를 수집한다. 수집 절차·기록지는 팀 노션의 「검증용 데이터 수집 가이드」 참조.

| | 경로 A | 경로 B |
|---|---|---|
| 출발 | 경희대학교 공과대학 정문 | 영통역 1번 출구 앞 |
| 도착 | 영통역 2번 출구 앞 | 레이지하우스 카페 |
| 거리 | 약 1.2km (네이버 안내 18분) | 답사 후 확정 |
| 신호 횡단보도 | 3개 | 답사 후 확정 |

**네이버 방식 확인**: 경로 A의 네이버 도보 안내값 1.2km = 18분을 역산하면 정확히 **1.111 m/s = 시속 4.00km**이며 신호대기는 가산되지 않는다. 본 보고서가 비교 기준으로 삼은 "정속 4km/h"가 실제 상용 서비스와 일치함이 확인되었다.

**경로 2개 설계의 이점**

경로가 1개면 그 경로만의 고유 오차(공사·인도 폭·해당 신호의 실제 주기 편차)와 모델 오차가 교란되어 분리할 수 없다. 측정을 아무리 늘려도 전부 같은 경로를 공유하므로 독립 표본이 아니다.

경로 2개에서는 오차 방향이 **두 경로에서 일관되면 모델 편향**, **갈리면 경로 특성**으로 판별할 수 있다. 검출력도 개선된다.

| 설계 | 측정 수 | 검출력(경로오차 ±25초) | 검출력(±50초) |
|---|---|---|---|
| 경로 1개 × 6명 × 2회 | 12 | 92% | 83% |
| **경로 2개 × 6명 × 2회** | **24** | **99%** | **88%** |

**남은 한계**

- 두 경로 모두 영통구 내 유사 지형이므로, 완전히 이질적인 환경(예: 급경사 위주 경로)에 대한 일반화는 여전히 제한적이다.
- 편도만 측정하므로 경사 방향이 고정된다. 왕복 측정 시 검증 가능했을 `k_slope` 비대칭(내리막 1.056배 / 오르막 0.944배)의 양방향 확인은 이번 설계에서 제외한다.

**분석 계획**: 경로별로 사람별 MAE·MAPE를 먼저 산출하고, 두 경로의 오차 패턴을 비교해 모델 편향과 경로 특성을 분리한 뒤, 전체 24개로 정속 방식 대비 통계 검정을 수행한다.

## 7. 신호대기 모델은 현 데이터로 검증 불가

신호 데이터가 커버하는 영통구 내 팀원(권동하·김재형·박준서)의 학습 경로 49개를 전수 조사한 결과, **횡단보도 반경 25m 내를 지나는 경로가 0개**였다.

| 팀원 | 경로에서 가장 가까운 신호 횡단보도까지 |
|---|---|
| 권동하 | 32 m (100m 내 4개) |
| 박준서 | 159 m |
| 김재형 | 655 m |

경사도 측정을 위해 언덕길만 선별해 걸었으므로 당연한 결과다. 따라서 **신호대기 계수의 정확도는 학습 데이터로 검증할 수 없으며**, 6절의 검증 경로 수집이 유일한 검증 수단이다.

참고로 신호대기 문서는 자체 한계로 "균등도착 가정 미검증", "타 시군 대조 시 대기를 약 +7% 과대추정" 등을 명시하고 있어, 검증 경로의 스톱워치 실측이 이 편향을 확인하는 자료가 된다.

## 8. 남은 한계 (정직하게 기재할 것)

**검증 완료 후 갱신 (11절 반영)**

- **콜드스타트는 통계적으로 유의하지 않다** (43% 감소, p=0.055). 처음 보는 사용자에 대해서는
  정속 대비 우위를 주장할 수 없다. 원인은 v_user의 학습 맥락 불일치이며(1절), 박준서 한 명은
  정속보다 나빴다. 서비스화 시 신규 사용자는 개인화를 끄고 시작해야 한다.
- **웜스타트 결과는 표본 4명·12회차에 기반한다.** 사람당 이력 3회로 캘리브레이션했고,
  홍민기는 캘리브레이션 후 오히려 나빠졌다(53초 → 59초) — 회차 간 변동이 큰 사람에게는
  평균 3회로 부족하다.
- **경사 모델이 방향 비대칭의 24%만 설명한다**(11-3절). 검증 경로가 완경사(±1~2%)라
  1차식 보정폭이 작다. 급경사 왕복 설계로 재측정 필요.
- **신호대기는 실측을 넣은 수치다.** 공공데이터만으로는 실측의 절반 이하로 과소추정되며
  (69~84초 vs 156~193초) 그 조건의 MAE는 84초 → 99초다(11-5절).
- **권동하·박준서 세그먼트는 DEM 대리면으로 처리했다**(11-0절). ETA 영향은 1초 미만으로
  검증했으나, DEM 원본이 있는 환경에서 재생성해 대조하는 것이 원칙이다.
- **누적거리에 사람별 GPS 지터 편향이 있다**(최대 34% 차이, 11-5절). 상대 비교는 견고하나
  절대 거리 인용은 피해야 한다.
- 급경사 내리막(≤ −8%)의 가속을 1차식이 포착하지 못함 (속도 모델 문서의 한계 승계).
- 측정 구간의 53%가 −2~+2% 평지대에 집중되어 경사 축 변동 폭이 좁다.
- **전처리의 정지구간 감지가 1초 간격 데이터에서 작동하지 않음.** `segmentation.py`의 판정 규칙이
  `step_time_s >= 3초 AND 이동 < 1m`인데, 1초 간격 기록에서는 각 스텝이 1초이므로 조건이
  성립하지 않는다(60초를 서 있어도 "1초짜리 스텝 60개"로 기록됨). 실제로 134개 파일 27,208개
  포인트 중 정지 판정은 **1개**뿐이었다. 이 규칙은 "사람이 멈춘 것"이 아니라 "기록이 끊긴 것"을
  잡고 있다. **신호 대기는 GPX만으로 복원할 수 없으며 스톱워치 실측이 정답이다.**
- 계단은 DEM 경사도에 반영되지 않아 모델이 포착하지 못한다.

## 9. 재현 방법

```bash
python -m src.eta                              # ETA 엔진 동작 확인
python -m src.demo                             # ETA 계산기 직접 사용 (거리·경사 입력)
python -m src.evaluate_internal --n-seeds 10   # 4절 내부 검증 (표 재생성)
python -m src.evaluate_internal --shrinkage-curve --n-seeds 20  # 4-1절 손익분기점 실험
python -m src.evaluate_internal --learning-curve --n-seeds 40    # 4-2절 개인화 학습곡선

# 최종 평가 (11절) — 검증 GPX 16개 기준
python -m src.build_dataset --raw-dir data/raw/validation --dem-dir data/raw/dem \
  --out data/processed/validation_segments.csv
python -m src.evaluate_validation --segments data/processed/validation_segments.csv
python -m src.plot_validation                  # 그림 3장 생성
python -m src.evaluate_validation --dry-run    # 동작 확인용
```

산출물: `data/processed/eval_internal.csv`, `eval_learning_curve.csv`, `validation_results.csv` (그래프용 원자료)
그림: `figures/fig_검증_사람별오차.png`, `fig_검증_요인기여도.png`, `fig_검증_캘리브레이션.png`

## 10. 검증 데이터 도착 시 실행 절차

1. GPX 24개를 `data/raw/validation/` 에 배치 (파일명 `이름_검증A_1회차.gpx` 형식)
2. 노션 기록지의 신호대기 실측을 `data/processed/validation_stopwatch.csv`로 정리

   ```csv
   person,route,trial,wait_s
   권동하,검증A,1,97
   권동하,검증A,2,55
   ```

   `wait_s` = 그 회차의 신호대기 시간 **총합**(초). 이 파일이 없으면 공공데이터
   기대대기값으로 대체 계산되며, 있으면 실측값이 우선한다.
3. `python -m src.evaluate_validation --dem-dir data/raw/dem` 실행

자동 산출되는 것:

| 출력 | 내용 |
|---|---|
| [1] 사람별 오차 | 회의 결정대로 풀링 없이 6명 각각 MAE·MAPE (경로별 분리) |
| [2] 요인별 기여도 | M0 정속 → M1 +경사 → M2 +개인화 → M3 +신호 단계별 오차 감소폭 |
| [3] 경로 간 비교 | 두 경로의 부호 오차가 일치하면 모델 편향, 갈리면 경로 특성으로 판별 |
| [4] 통계 검정 | 대응표본 t-검정 + Wilcoxon 병기 |



---

## 11. 최종 검증 결과 — 독립 경로 16회차

### 11-0. 데이터와 처리

| | |
|---|---|
| GPX | `data/raw/validation/` 16개 (4명 × 경로 2개 × 2회) |
| 구간 | `data/processed/validation_segments.csv` 509개 |
| 신호대기 | 스톱워치 실측 16건, 평균 176초 (총 소요시간의 18%) |
| 정답 | `dt_s` 총 경과시간 |

**⚠️ DEM 대체 처리 (재현 시 주의).** 권동하·박준서 8개 파일은 DEM 원본이 없는 환경에서
처리했다. 같은 경로를 걸은 최희수·홍민기 세그먼트의 DEM 샘플값 552점 + 인근 학습 세그먼트
366점(중복 병합 후 918점)으로 국소 DEM 대리면(가우시안 가중 평면 적합)을 만들어 고도를
샘플링했다. leave-one-file-out 검증 결과:

| 항목 | 값 |
|---|---|
| 고도 재현 | MAE 0.145 m, RMSE 0.289 m, bias +0.02 m |
| 경사도 재현 | MAE 0.614 %p |
| **최종 ETA 영향** | **평균 0.4초, 최대 0.9초** (모델 MAE 28초 대비 무시 가능) |

병합 후 기존 8회분 결과(179초 → 68초, 62% 감소)가 그대로 재현되는 것도 확인했다.
DEM 원본이 있는 환경에서는 정규 파이프라인으로 재생성해 대조할 것을 권한다.

### 11-1. 왕복 혼입 — 실제로는 경로가 4개였다

수집 설계는 편도 2경로였으나, **박준서·홍민기는 2회차를 돌아오는 길로 걸었다**(역방향 4회).
좌표로 확정했다 — 두 사람의 2회차는 기준 출발점에서 712~967m, 도착점에서 3~58m 지점에서
시작하며 고도 방향도 뒤집혀 있다.

| | 검증1 1회차 | 검증1 2회차 | 검증2 1회차 | 검증2 2회차 |
|---|---|---|---|---|
| 권동하 | 정방향 | 정방향 | 정방향 | 정방향 |
| 최희수 | 정방향 | 정방향 | 정방향 | 정방향 |
| 박준서 | 정방향 | **역방향** | 정방향 | **역방향** |
| 홍민기 | 정방향 | **역방향** | 정방향 | **역방향** |

모델은 GPX 트랙에서 경사를 읽고 정답은 그 회차의 실제 소요시간이므로 **회차 하나하나는
그대로 유효한 테스트 케이스**다. 영향을 받는 것은 집계 방식뿐이라, 주분석은 설계대로 걸은
**정방향 12회**로 하고 역방향 4회는 11-3절에서 따로 다룬다.

결과적으로 역방향 회차는 네이버의 경사 반영 여부를 확인하는 자료가 됐다. 팀에서 네이버
역방향 길찾기를 대조해 **"경사는 반영되나 가중치가 과대"**라는 결론을 얻었다(11-4절).

### 11-2. 주결과 — 요인별 기여도 (정방향 12회)

| 단계 | MAE | MAPE | 직전 대비 |
|---|---|---|---|
| M0 정속 4km/h | 147초 | 16.4% | — |
| M1 +경사 (팀평균 속도) | 136초 | 13.4% | +10초 |
| M2 +개인화 (학습 v_user) | 136초 | 13.6% | ±0초 |
| M3 +신호 (실측) | 84초 | 9.1% | +53초 |
| **M4 +개인 캘리브레이션** | **28초** | **2.9%** | **+56초** |

- 신호대기가 단일 최대 기여 요인(+53초). 실측 평균 176초로 총 소요시간의 18%인데
  기존 지도앱은 이를 전혀 반영하지 않는다.
- **M2(학습 v_user 개인화)는 기여가 0이다.** 도메인 시프트 때문이며, 같은 개인화라도
  맥락이 맞는 이력으로 하면(M4) 56초를 줄인다.
- 사람별로는 최희수 261초→28초, 권동하 70초→18초, 홍민기 98초→59초.
  박준서는 콜드스타트에서 정속보다 나빴다가(135초 vs 94초) 캘리브레이션 후 23초로 회복.

그림: `figures/fig_검증_요인기여도.png`, `fig_검증_사람별오차.png`, `fig_검증_캘리브레이션.png`

### 11-3. 왕복 대칭 — 경사 모델은 방향 비대칭을 과소보정한다

역방향 4회는 같은 길을 반대로 걸은 것이라 경사 효과를 직접 잰다. 생속도 차이는
(경사 + 그 외)이고, 평지환산 속도 차이는 (그 외)만 남는다.

| 사람 | 경로 | 왕복 생속도 차 | k_slope 보정 후 | 설명한 몫 |
|---|---|---|---|---|
| 박준서 | 검증1 | 4.0% | 1.4% | 2.6%p |
| 박준서 | 검증2 | 5.4% | 4.0% | 1.4%p |
| 홍민기 | 검증1 | 13.4% | 11.5% | 1.9%p |
| 홍민기 | 검증2 | 7.1% | 6.0% | 1.1%p |
| **평균** | | **7.5%** | **5.7%** | **24%만 설명** |

**k_slope가 왕복 비대칭의 24%밖에 설명하지 못한다.** 검증 경로의 평균 경사가 ±1~2%로
완만해 1차식 보정폭이 작은 것이 일차적 원인이고, 남은 비대칭은 계단·피로·노면 등
미모델링 요인으로 보인다. 급경사 구간을 포함한 왕복 설계로 재측정할 필요가 있다.

그림: `figures/fig_검증_왕복대칭.png`

### 11-4. 비교 baseline(M0)의 타당성

홍민기·박준서가 네이버 역산으로 확인한 바에 따르면 네이버는 **내리막·평지에서 신호대기를
반영하지 않고 시속 4km 정속**으로 계산하며, **오르막에서만 경사 가중치**를 준다(가중치는 과대).

검증 경로는 평지·완경사 위주(검증1 평지 46%·내리막 37%, 검증2 평지 73%)라, 우리 k_slope를
오르막에만 적용해 네이버를 모사해도 **M0 예측은 평균 +6초밖에 움직이지 않고 MAE는 130초 →
134초로 오히려 나빠진다.** 즉 현재의 단순 정속 M0는 네이버에게 유리하게 잡은 **보수적
baseline**이며, 네이버의 오르막 가중치가 과대하다면 실제 격차는 보고된 수치보다 크다.

**팀에 요청할 것**: 네이버 정/역방향 안내 시간 실측값이 있으면 M0를 실측 기반으로 교체할 수
있다. 현재는 "1.2km = 18분" 한 점만 확보되어 있다.

### 11-5. 이번 검증에서 드러난 데이터 품질 이슈

- **누적거리가 사람마다 34% 차이난다.** 시종점 직선거리는 전원 930±10m(검증1)로 동일해
  경로는 같다. 1초 간격 GPS 지터가 누적 haversine에 양의 편향을 만들며, 느리게 걷는 사람일수록
  심하다(우회율 홍민기 1.6~1.9 vs 박준서 1.2~1.5). 이 편향은 M0와 우리 모델 양쪽에 동일하게
  들어가고 v_hat 계산에서도 상쇄되므로 비교 자체는 견고하나, 절대 거리를 인용할 때는 주의해야 한다.
- **공공데이터 신호대기가 실측의 절반 이하다.** 모델 69~84초 vs 실측 156~193초. M3는 실측을
  사용하므로 위 수치에 영향이 없으나, 실서비스는 공공데이터를 써야 하므로 그 조건에서는
  MAE가 84초 → 99초로 나빠진다. 원인은 신호대기 문서의 미등록 횡단보도 3곳(최근접 62~118m)과
  `보행신호=무` 오기재 1곳으로 추정된다.

### 11-6. 재현

```bash
# DEM 원본이 있는 환경
python -m src.build_dataset --raw-dir data/raw/validation --dem-dir data/raw/dem \
  --out data/processed/validation_segments.csv

python -m src.evaluate_validation --segments data/processed/validation_segments.csv
python -m src.evaluate_validation --segments ... --all-runs   # 역방향 포함 16회 통합
python -m src.plot_validation                                 # 그림 4장
```

---

## 변경 이력

- 2026-08-22: 최초 작성. ETA 통합 엔진 구현, 회차 단위 홀드아웃 검증 도입,
  v1/v2 비교 평가, shrinkage 미채택 결정
- 2026-08-22: 4-1절 보강. shrinkage 미채택 근거를 "개선 없음"에서 표본 크기별
  손익분기점(n≈10) 규명으로 정정하고, 소표본 상황 확장 권고 추가
- 2026-08-22: 신호대기 산출물 연동. 6-2절 추가(학습 경로에 횡단보도 0개 —
  신호 모델 검증 불가 확인). 6-1절 검증 수집 설계 확정(공대↔영통역 왕복, 12개 GPX).
  전처리 정지구간 감지 결함 및 계단 미반영을 한계에 추가
- 2026-08-22: 검증 수집 설계를 독립 경로 2개(공대→영통역2출 / 영통역1출→레이지하우스)
  × 6명 × 2회 = 24개로 확정. 경로 고유 오차 분리 가능, 검출력 92%→99%
- 2026-08-22: `src/evaluate_validation.py` 추가(검증 데이터 최종 평가 자동화).
  유효 경사 범위는 계수 파일(`k_slope_model_v2.json`) 기준으로 적용
- 2026-08-22: 4-2절 추가. 개인 데이터량 학습곡선으로 "정확도는 30개에서 포화,
  신뢰구간은 계속 축소(홍민기 ±69초→±8초)" 확인. 개인화 가치는 데이터 양이 아니라
  팀평균과의 이탈 정도가 결정함을 정량화
- 2026-08-24: **11절 최종 검증 결과 추가 (검증 16회차 완결).** 권동하·박준서 세그먼트를
  DEM 대리면으로 처리(ETA 영향 <1초, LOO 검증). 헤드라인을 정방향 12회 기준
  147초→28초(81% 감소, p=0.0016)로 갱신. 왕복 혼입 4회 발견 및 분리, k_slope의
  방향 비대칭 설명력 24% 규명, 네이버 baseline 타당성 검토.
  `src/plot_validation.py` 추가(그림 4장)
- 2026-08-24: **핵심 발견 정립 — 개인화의 성패는 v_user의 학습 맥락이 좌우한다.**
  학습 v_user(경사 실험 반복 보행)와 검증 맥락(연속 목적지 보행)의 괴리가 최대 21%.
  콜드스타트 43% 감소(p=0.055, 유의 미달) vs 웜스타트 81% 감소(p=0.0016).
  누수 없는 회차 leave-one-out 캘리브레이션(M4)을 평가 단계에 정식 추가
