# WalkFit — 개인 맞춤형 도보 ETA 예측

KHUDA 10기 토이프로젝트 2팀. 이 문서는 Claude Code 세션 시작 시 읽는 프로젝트 컨텍스트입니다.

## 무엇을 만드는가

기존 지도 서비스는 도보 시간을 **모두에게 시속 4km 정속**으로 계산하고 경사·신호를 무시합니다.
이 세 요인을 실측 데이터로 보정한 개인 맞춤 ETA 모델을 만듭니다.

```
ETA = Σ [ 구간거리 ÷ (v_user × k_slope(경사도)) ] + Σ 신호대기
```

**검증된 사실**: 네이버지도 도보 안내(1.2km = 18분)를 역산하면 정확히 1.111 m/s = 시속 4.00km이며
신호대기를 가산하지 않습니다. 비교 기준 "정속 4km/h"는 실제 상용 서비스와 일치합니다.

## 담당 (권동하) — 평가·검증·통합

세 파트의 산출물을 하나의 ETA 엔진으로 조립하고, 모델 성능을 통계적으로 검증합니다.
속도 모델(k_slope, v_user)과 신호대기 모델은 다른 팀원이 만듭니다.

### 내가 만든 파일

| 파일 | 역할 |
|---|---|
| `src/eta.py` | ETA 통합 엔진. 계수 파일을 자동 인식해 조립 |
| `src/evaluate_internal.py` | 학습 데이터 내부 검증 (홀드아웃 / shrinkage / 학습곡선) |
| `src/evaluate_validation.py` | 검증 경로 최종 평가 |
| `src/plot_validation.py` | 검증 결과 시각화 5장 |
| `docs/평가_검증_보고서.md` | 검증 방법론·결과·한계 |

다른 팀원이 `src/route_wait.py`(횡단보도 통과 판정)를 추가하고 `eta.py`의 신호 판정을 그쪽에 위임하도록 수정했습니다.

## 현재 상태 (2026-08-24) — 담당 파트 완료

### 최종 결과 (6명 24회차, 주분석은 정방향 18회)

신호대기를 **공공데이터로 예측**한 것이 대표값이다. 스톱워치 실측을 넣으면 신호 항의
오차가 정의상 0이 되어 대표값으로 못 쓴다(박준서 지적, `docs/신호대기_ETA_대조.md`).

| 무엇을 켰나 | 실서비스 | 상한선(실측) |
|---|---|---|
| 기존 지도앱 (정속 4km/h) | 140초 | 140초 |
| + 경사 | 146초 | 146초 |
| + 개인 속도(기존 실험값) | 135초 | 135초 |
| + 신호대기 | 100초 | 95초 |
| **+ 개인 속도(실제 이력)** | **74초** | **47초** |

- 처음 보는 사용자: 29% 감소, t 6.7% ❌ / Wilcoxon 28.4% ❌
- **이력 2회 이상: 47% 감소, t 3.9% ✅ / Wilcoxon 6.0% ❌ — 엇갈림**
- 상한선(신호 완벽): 66% 감소, 0.2% ✅

### 결론 세 줄

**① 기존 지도앱의 오차 두 개가 서로 상쇄된다.** 느린 속도 가정(+35%)과 신호대기
미반영(−17.9%). 그래서 **개인 속도만 고치면 140초 → 172초로 나빠진다.** 개인화와
신호대기는 반드시 같이 가야 한다.

**② 이력이 쌓이면 계속 좋아지나 포화점은 미확인.** 실서비스 100→91→81→74초
(3회에서도 개선 중). 상한선 조건에서는 84→34→28→28초로 2회면 포화.
단 "그 사람의 실제 이동" 기록이어야 한다 — 경사 실험용 200m 반복 17회보다
실제 이동 이력이 정확하다(박준서 119초 vs 60초).

**③ 걸음이 일정한 사람에게만 통한다.** 회차 간 속도 변동폭과 남은 오차의 순위가
6명 모두 일치(순위상관 1.00, 정확검정 p=0.0028).

| 사람 | 변동폭 | 오차 |
|---|---|---|
| 박준서 | 2.3% | 10초 |
| 권동하 | 3.8% | 18초 |
| 윤예진 | 7.0% | 28초 |
| 최희수 | 9.3% | 28초 |
| 홍민기 | 10.1% | 64초 |
| 김재형 | 31.3% | 115초 |

이건 전처리 문서 6절(김재형·박준서·홍민기가 계속 안 맞음)과 모델링 문서 4절
(개인별 설명력 최하위 김재형·홍민기)이 각자 발견한 것과 같은 사람들이다.
→ 원인은 "경사 반응이 특이해서"가 아니라 **"걸음이 일정하지 않아서"**다.

### 완료

- ETA 엔진, 신호대기 연동, 내부 검증(61% 감소)
- 검증 GPX 24개 세그먼트 처리 → `validation_segments.csv` 743구간
- `evaluate_validation.py`: 개인 이력 재추정, 정/역방향 자동 판정, 왕복 대칭 분석
- `plot_validation.py`: 그림 5장
- `docs/평가_검증_보고서.md` 1·8·11·12절 작성

## 반드시 알아야 할 것 (검증에서 새로 확인)

**⑥ 24회 중 6회는 역방향이다.** 박준서·홍민기·윤예진이 2회차를 돌아오는 길로 걸었다.
좌표로 확정. 회차 자체는 유효하니 버리지 말 것. 주분석은 정방향 18회,
역방향은 경사 방향 효과 분석에 쓴다. `direction_of()`가 자동 판정한다.

**⑦ 김재형 파일 라벨이 경로1↔경로2로 뒤바뀌어 있었다.** 고도·좌표·연속보행 시각
세 가지로 확정. `validation_segments.csv`와 스톱워치 CSV에서 교정 완료.
원본 GPX 파일명은 그대로 두었다. **본인 확인 필요.**

**⑧ DEM 원본이 없는 환경에서는 대리면으로 처리했다.** 권동하·박준서·김재형 12개
파일 해당. 기존 DEM 샘플로 국소 가중 평면 적합. 검증 결과 ETA 영향 최대 0.9초.

**⑨ 경사는 이번 검증으로 못 쟀다.** 순수 기여 −1초, 왕복 비대칭 설명력 14%.
검증 경로가 평지 위주(46%/73%)라 경사 축 변동이 부족. "효과 없음"이 아니라 "미검증".

**⑩ 비교 기준(정속 4km/h)은 보수적이다.** 팀 역산 결과 네이버는 내리막·평지에서
신호 미반영 정속, 오르막에만 과대한 경사 가중치. 검증 경로가 평지 위주라 경사 가중을
모사해도 기준이 +6초만 움직인다.

**⑪ 누적거리에 사람별 GPS 지터 편향이 최대 34%.** 직선거리는 전원 동일(930±10m).
비교는 견고하나 절대 거리 인용 금지.

**⑫ 김재형은 23:27~04:43에 측정했다.** 새벽 4시 회차가 가장 느리다(1.090 vs 1.499).
시간대·피로 미통제. 기록지 총시간도 GPS와 40~130초 어긋나 신호대기만 사용했다.

**⑬ 신호대기 대표값은 예측이지 실측이 아니다.** 스톱워치 실측을 넣으면 정답에서
신호 항이 상쇄돼 오차가 정의상 0이 된다. 평가 스크립트가 두 갈래(실서비스/상한선)를
나란히 출력한다. 발표 수치는 실서비스(74초).

**⑭ 신호대기 보정계수 λ=0.745는 검증 4명 16회차로 맞춘 값이다.** 김재형·윤예진이
깨끗한 홀드아웃이며(λ 산출 시점에 없었음) 편향 −9%, MAE 29초로 보정군(−7%, 52초)과
동등 이상 → 과적합 아님. 단 다른 지역에 전용 불가.

**⑮ 신호대기는 이미 이론적 한계에 가깝다.** 같은 경로 실측이 67~243초로 흔들려,
경로 평균을 완벽히 안다고 가정해도 회차당 41초가 남는다. 현재 모델 44초 —
개선 여지 3초. 정교화보다 신뢰구간 표시가 실용적이다.

## 다음에 할 일

- 김재형에게 파일 라벨 교체 알리기 (경로1↔경로2)
- (선택) 네이버 정/역방향 실측 안내시간 수집 → 비교 기준 교체

## 실행 방법

```bash
python -m src.eta                                                # 엔진 동작 확인
python -m src.evaluate_internal --n-seeds 10                     # 내부 검증
python -m src.evaluate_internal --shrinkage-curve --n-seeds 20   # shrinkage 손익분기점
python -m src.evaluate_internal --learning-curve --n-seeds 40    # 개인화 학습곡선
python -m src.evaluate_validation --segments data/processed/validation_segments.csv
python -m src.evaluate_validation --segments ... --all-runs      # 역방향 포함 24회
python -m src.plot_validation                                    # 그림 5장
python -m src.evaluate_validation --dry-run                      # 검증 평가 동작 확인
```

## 반드시 알아야 할 것 (기존 함정 ①~⑤)

**① 실측 정답은 `dt_s`(총 경과시간)이다.**
`moving_dt_s`(정지 제외)를 쓰면 신호 대기가 정답에서 빠져 검증이 무의미해집니다.

**② 전처리의 정지구간 감지가 1초 간격 데이터에서 작동하지 않는다.**
`segmentation.py`의 규칙이 `step_time_s >= 3초 AND 이동 < 1m`인데, 1초 간격 기록에서는
각 스텝이 1초라 조건이 성립하지 않습니다. 실제로 134개 파일 27,208개 포인트 중 정지 판정은
**1개**뿐이었습니다. 이 규칙은 "사람이 멈춘 것"이 아니라 "기록이 끊긴 것"을 잡습니다.
→ **신호 대기는 GPX로 복원 불가. 스톱워치 실측이 정답입니다.**
(박준서 기록지도 같은 결론: GPX 검출이 매번 3~5초/지점씩 작게 나옴)

**③ 검증은 회차(파일) 단위로 분할해야 한다.**
같은 GPX의 인접 구간은 강하게 상관되어 있어, 구간 단위 랜덤 분할은 데이터 누수입니다.
또한 배포된 `v_user_v2.csv`를 그대로 쓰면 test를 포함해 계산된 값이라 누수 → 매 분할마다
train만으로 전체 파이프라인을 재적합해야 합니다.

**④ 계단은 모델에 반영되지 않는다.** DEM 경사도로만 계산하므로 계단 구간은 미모델링 오차입니다.

**⑤ Strava 자동 일시정지를 끄지 않으면 데이터가 무효다.** 신호 대기가 GPX에서 사라집니다.
수집된 16개 GPX는 전부 확인 완료(기록 간격 1초, 3초 이상 공백 0건).

## 검증된 실험 결과 (재현 가능)

**Shrinkage 손익분기점**: 표본이 줄수록 shrinkage가 유리해지며 5개 구간 부근에서 부호 역전.
본 프로젝트는 최소 72개라 **미적용이 타당**. 서비스화 시 이력 10개 미만 사용자에겐 적용 권고.

**개인화 학습곡선**: 예측오차는 20~30개 구간에서 포화(v_user는 평균 하나라 1/√n 수렴).
반면 ETA 신뢰구간은 계속 축소 — 홍민기 ±69초 → ±8초. 더 모아서 얻는 것은
"더 정확한 값"이 아니라 **"그 값을 더 확신할 수 있다"**는 점.

**개인화의 가치는 데이터 양이 아니라 평균에서의 이탈이 결정**: 최희수(팀평균 +18.3%)는
5개 구간만으로 오차 56% 감소. 평균에 가까운 인원은 아무리 모아도 1~5%.

## 데이터 위치

| 경로 | 내용 |
|---|---|
| `data/raw/*.gpx` | 학습용 보행 데이터 134개 |
| `data/raw/validation/` | 검증 경로 GPX 24개 + 기록지 |
| `data/raw/dem/` | 5m DEM (gitignore — 일부 로컬에만 존재, ⑦ 참조) |
| `data/processed/segments.csv` | 학습 구간 969개 |
| `data/processed/validation_segments.csv` | 검증 구간 743개 (24개 파일 전량) |
| `data/processed/validation_stopwatch.csv` | 신호대기 실측 24건 |
| `models/k_slope_model_v2.json` | 경사 계수 (1차식) |
| `models/v_user_v2.csv` | 개인별 보행속도 |

## 검증 경로

| | 경로1 | 경로2 |
|---|---|---|
| 출발 | 경희대 공과대학 정문 | 영통역 1번 출구 |
| 도착 | 영통역 2번 출구 | 레이지하우스 카페 |
| 거리 | 약 1.2km (네이버 18분) | 약 1.1km |
| 신호 횡단보도 | 4곳 | 5곳 |
| 정방향 경사 | 내리막 (−27m) | 오르막 (+13m) |

박준서·홍민기의 2회차는 역방향(돌아오는 길)이다. 위 ⑥ 참조.

## 팀원별 v_user (m/s)

최희수 1.627 / 권동하 1.360 / 윤예진 1.335 / 홍민기 1.332 / 박준서 1.301 / 김재형 1.294
(팀평균 1.375, 최대 차이 25.7%. 참고: 네이버 가정 1.111)
